require "spec_helper"

describe CircleciDeploymentNotifier do
  it "has a version number" do
    expect(CircleciDeploymentNotifier::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
